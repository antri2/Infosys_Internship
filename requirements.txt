Flask==3.0.3
PyMySQL==1.1.1
cryptography==43.0.1
python-dotenv==1.0.1
Werkzeug==3.0.4

# Resume parsing
PyMuPDF==1.24.10
python-docx==1.1.2

# LLM -- Groq's free hosted API, no local model download, no torch needed
groq==0.11.0
httpx==0.27.2

pytest==8.3.3

# RAG pipeline (M2.2) -- fastembed uses ONNX Runtime, not torch, so this
# stays lightweight (~130MB model download, one time, then fully offline)
fastembed==0.3.6
faiss-cpu==1.8.0